IMAP query scan-limit tests now exercise the production UID traversal through a deterministic
mailbox seam, so CPU load cannot change the expected `RESOURCE_LIMIT` classification while real
protocol and transport-failure coverage remain separate.
