Agent authority now verifies pinned policy and rate-card fingerprints before new effects, including approval consumption and dispatch. Legacy v1 budgets remain readable and support cleanup, but cannot authorize new effects without trustworthy proof; no fingerprints are backfilled from current configuration. New v2 aggregate writes form a downgrade boundary while v2 rows remain. See the [Agent budget compatibility notes](../docs/operator-guide/persistence-lifecycle.md#agent-budget-compatibility).
