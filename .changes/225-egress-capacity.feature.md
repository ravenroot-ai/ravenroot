Node package registration now captures the exact service view’s explicit egress capacity profile and refuses conflicting repeated package bindings. Operators can set the HTTP gzip expansion ratio in existing service-grant JSON; caller limits and legacy response handling remain intact. The [service-grant documentation](../docs/operator-guide/plugin-bundles.md#grant-required-runtime-services) also clarifies advisory cancellation and permit retention while an HTTP or credential worker is still running.
