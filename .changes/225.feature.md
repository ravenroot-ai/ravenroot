Add a reproducible whole-repository operational-configuration inventory and drift guard so every fixed candidate is reviewed before it can become an undocumented deployment policy. Startup parsers keep their documented defaults while rejecting malformed explicit authentication, SSE, assistant, execution-store, artifact-lifecycle, and unknown-behavior values. Graph limits now expose the eleven previously embedded structural budgets through bounded environment bindings, and graph-limit parsing rejects out-of-range values without retaining raw input in diagnostics.
