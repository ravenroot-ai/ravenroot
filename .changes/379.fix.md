The render and arrange mode chosen for a graph is now saved with it and restored when the graph is
reopened. Saved documents carry the choice in the `ravenroot.renderMode` and `ravenroot.layoutMode`
graph properties.
