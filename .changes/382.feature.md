The graph editor has two new visual node types, `trace` and `human-task`, each with its own icon,
shape and colour in every renderer. Both can be selected in the Inspector, and a node whose behaviour
is `human-task`, or `trace`, `log` or `logger`, gets the matching type by default.
