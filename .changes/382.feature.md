The graph editor has two new visual node types, `trace` and `human-task`, which can be chosen for a
node in the Inspector. In the standard graph style `trace` is drawn as a rectangle and `human-task` as
an ellipse.
