Operators can run workspace agents through a governed local or remote runner plane with durable job
state, policy and authorization checks, recovery-aware process continuation, bounded artifact handling,
HTTP and Java APIs, Workbench visibility, and documented container and quota controls.
