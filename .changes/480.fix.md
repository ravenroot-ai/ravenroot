# Hermetic MinIO acceptance supply

- Mirror the real MinIO server and client acceptance fixtures into a public Ravenroot-owned GHCR
  package, pin both by digest, and fail before the long backend build when either immutable image or
  the GitHub runner architecture is unavailable.
- Keep the TLS, SigV4, versioning, pagination, durable-restart, and cleanup gate backed by the real
  provider while recording reproducible provenance and rotation instructions.
