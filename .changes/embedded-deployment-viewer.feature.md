Embedded viewers can now attach to a running deployment for a read-only graph view with bounded live lifecycle and execution updates, while existing immutable snapshot registrations remain supported.
