apiVersion: v1
kind: Service
metadata:
  name: {{ include "ravenroot.fullname" . }}
  labels:
    {{- include "ravenroot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ravenroot.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
